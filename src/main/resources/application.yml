server:
  port: 10001
spring:
  application:
    name: shiro-server
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: org.gjt.mm.mysql.Driver
    url: jdbc:mysql://localhost:3307/shiro?useUnicode=true&characterEncoding=utf-8&useSSL=false
    username: root
    password: root

mybatis:
  mapperLocations:
    classpath:mapper/*.xml
#  type-aliases-package: com.atguigu.springcloud.entities
logging:
  level:
    root: info
